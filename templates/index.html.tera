<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8" />
    <title>Goldfinger</title>
  </head>
  <body>
    <form action="/lcd" method="post">
      <div>
        <select id="lcd-mode" name="mode">
          <option value="off" selected="{{ mode == "off" }}">Off</option>
          <option value="clock" selected="{{ mode == "clock" }}">Clock</option>
        </select>
      </div>

      <div>
        <input type="color" id="lcd-color" name="color" value="{{ color }}" />
        <label for="lcd-color">LCD Color</label>
      </div>

      <button type="submit">Save</button>
    </form>
  </body>
</html>
